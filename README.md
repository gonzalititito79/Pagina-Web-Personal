# Página Web Personal

Este repositorio contendrá la página web personal.

He creado este README como commit inicial para que podamos crear otras ramas y añadir el scaffold del sitio en una rama separada llamada `site-inicial`.

Próximo paso: crear la rama `site-inicial` y añadir `index.html`, `styles.css` y `assets/profile.svg` en esa rama.
